Check before run
================

* Check where is your mesh file
* Check what verion of openmpi (or other MPI library) are you using

Example Static (simple_elastcity)
==========================

mpirun -np 2 ./simple_elasticity -file_name simple_elasticity.h5m -ksp_type gmres -pc_type lu -pc_factor_mat_solver_package mumps -ksp_monitor -my_order 2
